import { locationAddressAddressInterface } from './location-address--address-interface';

describe('locationAddressAddressInterface', () => {
  it('should work', () => {
    expect(locationAddressAddressInterface()).toEqual(
      'location-address--address-interface'
    );
  });
});
